{% extends 'base.html.twig' %}

{% block body %}
    <h1>Product List</h1>

    <ul>
        {% for product in products %}
            <li>{{ product.name }} - {{ product.price }} USD</li>
        {% endfor %}
    </ul>
{% endblock %}
